replicaCount: 3

postgresql:
  enabled: false

externalDatabase:
  host: keycloak-postgres-known-jaybird.postgres.database.azure.com
  user: psqladmin
  password: "xRCvaM877*gePSeOuEdx@kOYdT"
  database: keycloak-db
  port: 5432

ingress:
  enabled: true
  ingressClassName: "nginx"
  pathType: ImplementationSpecific
  hostname: api.keycloak.full-coral.cc

adminIngress:
  enabled: true
  ingressClassName: "nginx"
  pathType: Prefix
  hostname: host.keycloak.full-coral.cc
